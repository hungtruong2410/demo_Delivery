name: CI - Full Pipeline (Build, Test, Monitor)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  integration-pipeline:
    runs-on: ubuntu-latest

    # 1. BIẾN MÔI TRƯỜNG TOÀN CỤC (Để tất cả các bước đều nhận được)
    env:
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      DB_NAME: ${{ secrets.DB_NAME }}
      NODE_ENV: test
      # Biến này giúp fix lỗi connect localhost trên một số runner
      mysql_host: 127.0.0.1 

    # 2. Dựng MySQL Service (Dùng bản 5.7 để ổn định nhất)
    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MYSQL_DATABASE: ${{ secrets.DB_NAME }}
        ports:
          - 3306:3306
        # Options đơn giản chỉ để Health Check (Không gây crash)
        options: >-
          --health-cmd="mysqladmin ping" 
          --health-interval=10s 
          --health-timeout=5s 
          --health-retries=5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci
          # Cài thêm các gói cần thiết nếu thiếu
          npm install jest-junit cross-env --no-save 

      # 3. Chờ MySQL thực sự sẵn sàng
      - name: Wait for MySQL Ready
        run: |
          for i in {1..30}; do
            if mysqladmin ping -h 127.0.0.1 -P 3306 -u root -p"${{ secrets.DB_PASSWORD }}" --silent; then
              echo "✅ MySQL is up!"
              break
            fi
            echo "Waiting for MySQL..."
            sleep 2
          done

      # 4. Import Database (Chạy trước khi Test)
      - name: Import Database Schema
        run: |
          sudo apt-get install -y mysql-client
          # Kiểm tra file .sql, nếu nằm trong thư mục database/ thì sửa đường dẫn bên dưới
          mysql -h 127.0.0.1 -P 3306 -u root -p"${{ secrets.DB_PASSWORD }}" ${{ secrets.DB_NAME }} < database/FoodOrderingWebsite.sql

      # 5. Chạy Test (Unit & Integration)
      - name: Run Tests
        run: npx jest --reporters=default --reporters=jest-junit --detectOpenHandles
        env:
          JEST_JUNIT_OUTPUT_DIR: "reports"
          JEST_JUNIT_OUTPUT_NAME: "junit.xml"
      
      # 6. Upload Datadog (Đã sửa tên gói chính xác)
      - name: Upload Test Report to Datadog
        if: always() 
        run: |
          npx @datadog/datadog-ci junit upload reports/junit.xml \
            --service food-ordering \
            --tags branch:${GITHUB_REF_NAME}
        env:
          DD_API_KEY: ${{ secrets.DD_API_KEY }}
          DD_SITE: us5.datadoghq.com

      # 7. Smoke Test (Khởi động server thật)
      - name: Start server and verify health
        run: |
          # Chạy server ở background
          nohup npm start > server.log 2>&1 &
          SERVER_PID=$!
          
          echo "Waiting for server to launch..."
          sleep 10
          
          # Kiểm tra server sống hay chết
          if curl --fail -I http://localhost:3000; then
            echo "✅ Server started successfully!"
          else
            echo "❌ Server failed to start. Logs:"
            cat server.log
            exit 1
          fi
          
          kill $SERVER_PID