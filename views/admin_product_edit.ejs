<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Edit Product</title>
  <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
  <style>
    .container{max-width:880px;margin-top:32px}
    .thumb{height:64px;object-fit:cover;border-radius:6px}
  </style>
</head>
<body class="container">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h3>Edit Product</h3>
    <div>
      <a href="/admin_products" class="btn btn-secondary btn-sm">Back</a>
      <a href="/logout" class="btn btn-outline-danger btn-sm">Logout</a>
    </div>
  </div>

  <form action="/admin_products/<%= item.item_id %>/edit" method="POST" enctype="multipart/form-data" class="row g-3">
    <div class="col-md-6">
      <label class="form-label">Name</label>
      <input name="item_name" class="form-control" value="<%= item.item_name %>" required>
    </div>
    <div class="col-md-3">
      <label class="form-label">Type</label>
      <input name="item_type" class="form-control" value="<%= item.item_type %>">
    </div>
    <div class="col-md-3">
      <label class="form-label">Category</label>
      <input name="item_category" class="form-control" value="<%= item.item_category %>">
    </div>

    <div class="col-md-3">
      <label class="form-label">Serving</label>
      <input name="item_serving" class="form-control" value="<%= item.item_serving %>">
    </div>
    <div class="col-md-3">
      <label class="form-label">Calories</label>
      <input name="item_calories" type="number" step="1" class="form-control" value="<%= item.item_calories %>">
    </div>
    <div class="col-md-3">
      <label class="form-label">Price</label>
      <input name="item_price" type="number" step="0.01" class="form-control" value="<%= item.item_price %>">
    </div>
    <div class="col-md-3">
      <label class="form-label">Rating</label>
      <input name="item_rating" type="number" min="1" max="5" class="form-control" value="<%= item.item_rating %>">
    </div>

    <div class="col-md-12">
      <label class="form-label">Current image</label><br>
      <img class="thumb" src="/images/dish/<%= item.item_img %>" alt="">
    </div>

    <div class="col-md-12">
      <label class="form-label">Upload new image (optional)</label>
      <input type="file" name="item_img" class="form-control">
      <div class="form-text">Nếu không chọn ảnh mới, ảnh cũ sẽ được giữ lại.</div>
    </div>

    <div class="col-12">
      <button class="btn btn-primary">Save changes</button>
      <a href="/admin_products/<%= item.item_id %>" class="btn btn-link">Cancel</a>
    </div>
  </form>
</body>
</html>
